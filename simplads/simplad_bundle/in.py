from .simplad_bundle import SimpladBundle
from .lift import lift

class in(object):
    def __init__(self, first):
        self.first = first
        self.sm = SimpladBundle()

    def add_error(obj):
        self.sm.add_error(obj)

    def pipe(*args):
        return SimpladBundle().unit(self.first).pipe([i for i in args])
